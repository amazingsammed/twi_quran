

import 'package:flutter/material.dart';

class SurahViewDrawer extends StatelessWidget {
  const SurahViewDrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return Drawer();
  }
}
